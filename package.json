{
  "name": "london-by-mood",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/worker.js && node --check public/app.js && node --check public/ranking.js"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
