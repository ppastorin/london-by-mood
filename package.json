{
  "name": "london-by-mood",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "db:migrate:local": "wrangler d1 migrations apply london-advanced-places-dev --local",
    "db:seed:local": "wrangler d1 execute london-advanced-places-dev --local --file db/seed.sql",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.mjs",
    "test:integration": "node scripts/integration-smoke.mjs",
    "test:csv": "node scripts/preview-csv-import.mjs",
    "check": "node --check src/worker.js && node --check src/migrate-production.js && node --check public/app.js && node --check public/ranking.js && node --check public/admin/app.js && node --check public/smart-navigation/app.js && python3 -m py_compile scripts/build-d1-seed.py"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
